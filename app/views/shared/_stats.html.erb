<% @user ||= current_user %>
  <a href="<%= following_user_path(@user) %>">
    <span class="badge badge-primary"><%= @user.following.count %></span>
    following
  </a>
  <a href="<%= followers_user_path(@user) %>">
    <span class="badge badge-primary"><%= @user.followers.count %></span>
    followers
  </a>
