<% @user ||= current_user %>
<a href="<%= user_following_path(@user) %>">
  <span class="badge badge-primary" id="following">
    <%= @user.following.count %>
  </span>
  following
</a>
<a href="<%= user_followers_path(@user) %>">
  <span
   class="badge badge-primary"
   id="followers">
    <%= @user.followers.count %>
  </span>
  followers
</a>
